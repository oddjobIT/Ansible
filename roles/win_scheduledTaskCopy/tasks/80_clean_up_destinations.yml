---
- name: 80 Delete import directory on each destination
  ansible.windows.win_file:
    path: "{{ import_dir }}"
    state: absent
  loop: "{{ destination_servers }}"
  loop_control:
    loop_var: dest_host
  delegate_to: "{{ dest_host }}"