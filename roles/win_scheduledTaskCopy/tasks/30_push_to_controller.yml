---
- name: 30 Push exported XML files to controller
  ansible.builtin.fetch:
    src: "{{ export_dir }}/{{ item | regex_replace('[\\\\/]', '_') }}.xml"
    dest: "{{ controller_dir }}/"
    flat: true
  loop: "{{ task_names }}"
  delegate_to: "{{ source_server }}"
